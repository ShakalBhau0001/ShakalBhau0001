name: 🐍 Neon Snake Animation Enhanced

on:
  schedule:
    - cron: "0 */3 * * *"
  workflow_dispatch:
  push:
    branches:
      - main
      - master

concurrency:
  group: snake-v2
  cancel-in-progress: true

jobs:
  build:
    name: 🧩 Build & Deploy Ultra Snake
    runs-on: ubuntu-latest
    if: github.repository == 'ShakalBhau0001/ShakalBhau0001'
    permissions:
      contents: write

    steps:
      - name: 🪶 Checkout Repository
        uses: actions/checkout@v4

      - name: ⚡ Setup Cache
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-snake-cache-v4

      - name: 🌈 Generate Ultra Neon Snake SVGs
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          color_snake: "#0011FF"
          color_dots: "#0011FF,#0011FF,#0011FF"
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
            dist/github-contribution-grid-snake-light.svg?palette=github-light
            dist/github-contribution-grid-snake-neon.svg

      - name: 🔧 Neon Blue Recolor + Background Fix
        run: |
          echo "🎨 Applying neon blue theme..."
          for f in dist/github-contribution-grid-snake*.svg; do
            # Snake + dots color
            sed -i 's/fill="[^"]*"/fill="#0011FF"/g' "$f" || true
            sed -i 's/stroke="[^"]*"/stroke="#0011FF"/g' "$f" || true

            # Force background to neon blue (#0011FF)
            sed -i 's/<rect width="100%" height="100%"[^>]*>/<rect width="100%" height="100%" fill="#0011FF" \/>/g' "$f" || true
          done
          echo "✅ Neon blue applied successfully!"

      - name: 🚀 Deploy Neon Blue Snake
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output-v2
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: ✅ Confirm Success
        run: |
          echo "🎉 Ultra Neon Blue Snake deployed successfully!"
          echo "🐍 Background + Snake + Dots all set to #0011FF!"
